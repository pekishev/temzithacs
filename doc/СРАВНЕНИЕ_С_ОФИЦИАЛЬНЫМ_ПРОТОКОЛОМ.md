# Сравнение протокола Android приложения с официальной документацией ТЭМЗИТ

**Источник документации:** [HM_Protocol.pdf](https://temzit.ru/downloads/HM_Protocol.pdf)

## Общие параметры

| Параметр | Официальная документация | Android приложение | Соответствие |
|----------|-------------------------|-------------------|--------------|
| IP адрес | 192.168.4.1 | 192.168.4.1 | ✅ Совпадает |
| Порт | 333 | Не указан явно | ⚠️ Не указан |
| Протокол | TCP Socket | TCP Socket | ✅ Совпадает |
| Размер ответа | 64 байта | 64 байта | ✅ Совпадает |

## Сравнение команд

### 1. SYNC / GET_STATE (0x30) - Запрос состояния

**Официальная документация:**
```
Запрос: [0x30] [0x00]  (2 байта)
Ответ: ACTUAL_STATE (0x01), 64 байта
```

**Android приложение:**
```java
GET_STATE = 48 (0x30)
Запрос: [0x30, 0x00]  (2 байта)
Ответ: тип 0x00 или 0x01, 64 байта
```

**Соответствие:** ✅ **ПОЛНОСТЬЮ СООТВЕТСТВУЕТ**

---

### 2. ACTUAL_STATE (0x01) - Ответ с данными состояния

**Официальная документация:**
```
[0x01] [0x00] [60 байт данных] [КС 2 байта]
КС = 16-разрядная сумма всех байт ответа
```

**Android приложение:**
```
[0x00 или 0x01] [резерв] [62 байта данных] [CRC 2 байта]
CRC = сумма байтов 0-61 (word, little-endian)
```

**Структура данных (сравнение):**

| Смещение | Официальная документация | Android приложение | Соответствие |
|----------|-------------------------|-------------------|--------------|
| 0-1 | состояние (16 бит) | state (word) | ✅ |
| 2-3 | Номер расписания (16 бит) | Schedule (word) | ✅ |
| 4-5 | Тулица * 10 (16 бит) | Tout (int16 / 10.0) | ✅ |
| 6-7 | Тдом * 10 (16 бит) | Tin (int16 / 10.0) | ✅ |
| 8-9 | Тподача * 10 (16 бит) | Tf (int16 / 10.0) | ✅ |
| 10-11 | Тобратка * 10 (16 бит) | Tb (int16 / 10.0) | ✅ |
| 12-13 | Тфреон газ * 10 (16 бит) | Tcond (int16 / 10.0) | ✅ |
| 14-15 | Тфреон жидк * 10 (16 бит) | Tevap (int16 / 10.0) | ✅ |
| 16-17 | Тгвс * 10 (16 бит) | Tgvs (int16 / 10) | ✅ |
| 18-19 | Проток (16 бит) | Flow (word) | ✅ |
| 20-21 | - | Резерв | - |
| 22 | Обороты компрессора 1 (8 бит) | CompFreq (младший байт word[24]) | ⚠️ Смещение |
| 23 | Обороты компрессора 2 (8 бит) | CompFreq2 (старший байт word[24]) | ⚠️ Смещение |
| 24-25 | Состояние ТЭНа (16 бит) | PowerTen (в коде не парсится явно) | ⚠️ |
| 26-27 | Состояние нагревателя в БКН (16 бит) | - | ❌ Не парсится |
| 28-29 | Потребление Вт / 100 (16 бит) | Pin (int16 / 10.0) | ⚠️ Формула |
| 30-31 | Авария (16 бит) | Failures (word) | ✅ |
| 32-42 | Резерв | Tf2, Tb2, Tcond2, Tevap2 (второй контур) | ⚠️ Расширение |
| 43 | Версия прошивки старшая | - | ❌ Не парсится |
| 44 | Версия прошивки младшая | - | ❌ Не парсится |
| 45 | Номер активного расписания | ReceivedPage | ✅ |
| 46 | Режим активного расписания | ModeSet | ✅ |
| 47 | 0 | 0 | ✅ |
| 48 | 0 | 0 | ✅ |
| 49 | Настройка Тдома | TroomSet | ✅ |
| 50 | Настройка Тводы | TwaterSet | ✅ |
| 51 | Настройка Тгвс | TgvsSet | ✅ |
| 52 | Ограничение ККБ | - | ⚠️ |
| 53 | Режим ТЭНа | - | ⚠️ |
| 54 | Режим ГВС | GVSModeSet | ✅ |
| 55 | 0 | 0 | ✅ |
| 56 | День недели | - | ❌ Не парсится |
| 57 | Часы | hour (BCD из байта 59) | ⚠️ Смещение |
| 58 | Минуты | minute (BCD из байта 60) | ⚠️ Смещение |
| 59 | Секунды | second (BCD из байта 61) | ⚠️ Смещение |
| 60-61 | - | Резерв | - |
| 62-63 | КС (16 бит) | CRC (word) | ✅ |

**Соответствие:** ⚠️ **ЧАСТИЧНОЕ СООТВЕТСТВИЕ**
- Основная структура совпадает
- Есть различия в смещениях некоторых полей
- Android приложение поддерживает расширенные функции (второй контур)

---

### 3. REQCFG / GET_SETTINGS (0x34) - Запрос настроек

**Официальная документация:**
```
Запрос: [0x34] [0x00]  (2 байта)
Ответ: CONFIG_MAIN (0x02), 64 байта
```

**Android приложение:**
```java
GET_SETTINGS = 52 (0x34)
Запрос: [0x34]  (1 байт)  ⚠️ ОТЛИЧИЕ!
Ответ: тип 0x02, 64 байта
```

**Соответствие:** ⚠️ **НЕБОЛЬШОЕ ОТЛИЧИЕ**
- Android приложение отправляет только 1 байт вместо 2
- Возможно, второй байт игнорируется устройством

---

### 4. CONFIG_MAIN (0x02) - Ответ с настройками

**Официальная документация:**
```
[0x02] [30 байт настроек] [31 байт резерв] [КС 2 байта]
```

**Android приложение:**
```
[0x02] [30 байт настроек в HEX] [резерв] [CRC 2 байта]
```

**Соответствие:** ✅ **СООТВЕТСТВУЕТ**

---

### 5. CFG / SEND_SETTINGS (0x35) - Изменение настроек

**Официальная документация:**
```
[0x35] [30 байт настроек] [КС 1 байт]
Размер: 32 байта
КС = сумма всех байт включая cmd
```

**Android приложение:**
```java
SEND_SETTINGS = 53 (0x35)
[0x35] [0x1A] [26 байт данных] [3 байта резерв] [CRC 1 байт]
Размер: 32 байта
CRC = сумма байтов 0-30
```

**Соответствие:** ⚠️ **СТРУКТУРНОЕ ОТЛИЧИЕ**
- Официально: 30 байт настроек напрямую
- Android: байт 1 = длина (26), затем 26 байт данных
- Возможно, Android использует устаревший формат или расширенный

---

## Дополнительные команды в Android приложении

Android приложение содержит дополнительные команды, не описанные в официальной документации:

1. **CHANGE_ONE_SETTING (0x32)** - Изменение одного параметра (8 байт)
2. **GET_RTCSETTINGS (0x36)** - Запрос настроек расписания
3. **SEND_RTCSETTINGS (0x37)** - Отправка настроек расписания (42 байта)
4. **CHANGE_ONE_RTCSETTING (0x33)** - Изменение одного параметра расписания
5. **GET_WIFISETTINGS (0x39)** - Запрос WiFi настроек
6. **SEND_WIFISETTINGS (0x3A)** - Отправка WiFi настроек (64 байта)

**Вывод:** Android приложение использует расширенную версию протокола с дополнительными функциями.

---

## Выводы

### ✅ Полное соответствие:
1. Базовые команды (SYNC, REQCFG)
2. Структура ответов (64 байта)
3. CRC проверка для ответов (16-битная сумма)
4. IP адрес и общий формат протокола

### ⚠️ Частичные отличия:
1. **GET_SETTINGS запрос**: Android отправляет 1 байт вместо 2 (возможно, второй байт игнорируется)
2. **SEND_SETTINGS структура**: Android использует формат с длиной данных (26 байт) вместо прямых 30 байт
3. **Смещения полей**: Некоторые поля имеют разные смещения (компрессор, время)
4. **Расширенные функции**: Android поддерживает второй контур и дополнительные настройки

### ❌ Отсутствующие поля в Android:
1. Версия прошивки (байты 43-44)
2. День недели (байт 56)
3. Некоторые настройки активного расписания (байты 52-53)

### 📝 Рекомендации:

1. **Для совместимости с официальным протоколом:**
   - Используйте порт **333** (указан в документации)
   - Для GET_SETTINGS отправляйте `[0x34, 0x00]` (2 байта)
   - Для SEND_SETTINGS используйте формат: `[0x35] [30 байт данных] [CRC 1 байт]`

2. **Для работы с Android приложением:**
   - Протокол в целом совместим
   - Учитывайте расширенные функции (второй контур, расписание)
   - Некоторые команды могут быть специфичны для Android версии

3. **Обновление документации:**
   - Рекомендуется обновить описание протокола с учетом найденных отличий
   - Добавить описание расширенных команд (RTC, WiFi настройки)
   - Уточнить формат SEND_SETTINGS

---

## Источники

- Официальная документация: [HM_Protocol.pdf](https://temzit.ru/downloads/HM_Protocol.pdf)
- Android приложение: декомпилированный код из `SimpleClient.java`

